/*
package com.example.gauthamhemanth.popularmoviesapp;


import android.app.Fragment;
import android.media.Image;
import android.support.annotation.Nullable;
import android.view.LayoutInflater;
import android.view.View;
import android.os.Bundle;
import android.view.ViewGroup;
import android.widget.GridView;
import android.widget.ImageView;

import com.squareup.picasso.Picasso;


*/
/**
 * Created by GauthamHemanth on 3/14/16.
 *//*

public class MovieGridFragment extends Fragment {

    @Override
    public View onCreateView(LayoutInflater inflater, ViewGroup container, Bundle savedInstanceState) {


        GridView gridView = (GridView)this.getActivity().findViewById(R.id.postergridview);
        MovieAdapter adapter = new MovieAdapter(this.getActivity());
        gridView.setAdapter(adapter);
        return inflater.inflate(R.layout.fragment_movie_grid, container,false);
    }

    @Override
    public void onActivityCreated(Bundle savedInstanceState) {
        super.onActivityCreated(savedInstanceState);
        onCreateView();


    }
}
*/
